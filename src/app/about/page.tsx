import { AboutContent } from "@/components/about/AboutContent";

export const metadata = {
  title: "About Us | PCCI",
  description: "Learn more about PCCI's history, mission, and commitment to excellence in construction and facility management.",
};

export default function AboutPage() {
  return <AboutContent />;
}
